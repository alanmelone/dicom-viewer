import { ShapeAttribute } from './shape-attribute.model';
import { RegionAttribute } from './region-attribute.model';

export class Region {
    // tslint:disable: variable-name
    shape_attributes: ShapeAttribute;
    region_attributes: RegionAttribute;
}
